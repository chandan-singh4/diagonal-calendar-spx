# backlog.md — TODOs, Enhancements, Bugs, Technical Debt, Future Ideas

**Last updated:** 2026-07-31
**Source:** `AUDIT_2026-07-25.md` §7 + follow-ups scattered through `DEV_JOURNAL.md`

Priority: **P0** blocks the current milestone · **P1** next milestone · **P2** scheduled ·
**P3** someday

> **OPEN items only — closing something means DELETING its row, never marking it fixed.**
> Git keeps the text (`git log -S "BUG-011" -- docs/backlog.md`). Git does *not* keep the
> reasoning: if the fix leaves a lesson or a ruled-out theory worth having later, write a
> short ADR in `decisions.md` first. Details: ADR-017.

---

## 🐞 Bugs

| ID | P | Item | Notes |
|---|---|---|---|
| BUG-001 | **P0** | **Unresolved v4.1.1 report: user said "still having some issue" with the canonical `app.py`; session ended before specifics were captured** | Needs exact symptom, which view/tab, and a screenshot. Per the 2026-07-07 journal: do NOT assume it is the same rendering bug — verify from evidence. Confirm the canonical file actually replaced the local one (look for `app_backup.py`), confirm a clean restart, confirm collector running. **Ruled out 2026-07-26 — see ADR-018:** the "two collector processes" Chandan reported this starting with are a single collector (a uv launcher plus the real interpreter). Not the cause; **still open, no leads.** |
| BUG-003 | P2 | Unsaved-changes guard false-positives on untouched forms | `st.form` only delivers values on submit, so the guard detects edit-mode *activation*, not field changes. Documented in `DOCUMENTATION.md` §12.8. Likely unfixable within Streamlit. |
| BUG-004 | P2 | `MARKET_HOLIDAYS` covers 2026 only | Will silently misclassify collection gaps from Jan 2027. Make multi-year or compute. |
| BUG-006 | P2 | `interpret_curve()` reports a NaN ratio as "Backwardation" | Found by the M1.2 tests, 2026-07-26. When `back_iv` is 0, `term_structure()` sets `ratio = NaN`. Every NaN comparison is False, so both branches in `iv_engine.py:117-122` fall through to the final `else` and the function states *"Backwardation (inverted) — front IV above back"* as fact, with no signal the input was unusable. **Fix:** guard `math.isnan(ts.ratio)` first and return an explicit "insufficient data" string. Current behaviour is pinned by `test_interpret_curve_reports_backwardation_for_a_nan_ratio` — fixing it must update that test deliberately. Low impact today (a 0 back IV would be a data fault, not a market state). |
| BUG-007 | P3 | Truthiness guards on floats treat a legitimate `0.0` as missing | Found by the M1.2 tests, 2026-07-26. `calendar_edge()` uses `if (fc.iv and bc.iv)` (`iv_engine.py:336-339`), so an IV of exactly `0.0` yields `edge=None`, indistinguishable from absent data. **No impact today** — 0.0 IV is not a physically meaningful SPX quote. Logged because the same `and`-on-floats pattern is a latent trap if copied to a quantity where `0.0` is valid; theta and edge both legitimately reach 0.0. **Fix:** compare `is not None` explicitly. Pinned by `test_calendar_edge_treats_zero_iv_as_missing`. **Now the last surviving site of this pattern** — the three in `db.py` were fixed 2026-07-26 (ADR-023 §2). |
| BUG-009 | P3 | `ic_max_profit` is rounded, and float error decides which way | Found by the M1.3 tests, 2026-07-26. `derive_ic()` does `max_p = round(locked * 100 * contracts)` (`pages/journal.py:592`) while nothing else in the returned dict is rounded. With credit 10.005 and debit 4.0, the locked value is 6.005000000000001 in binary float, so ×100 lands a hair above the midpoint and rounds **up** to 601 where exact arithmetic gives 600. Half a dollar is immaterial alone, but `ic_max_profit` feeds `ic_worst_case` and the `ic_risk_free` boolean, so a representation artefact reaches a displayed figure and a flag. Pinned by `test_derive_ic_max_profit_is_rounded_to_whole_dollars`. |
| BUG-010 | P3 | A flawless record and a missing calculation look identical on screen | `compute_stats()` returns `Profit Factor: None` when there are no losses, because the divisor is 0. Rendered as blank — indistinguishable from "not computed". Defensible, but worth an explicit "no losses" label. Pinned by `test_compute_stats_all_winners_gives_no_profit_factor`. |
| BUG-023 | **P1** | **The dashboard shows only the a.m. third-Friday option; the p.m. one is not displayed** | **Storage fixed 2026-08-19. Display fixed 2026-08-19 for the chain, the dropdowns, the IV history and both mark-history charts — see "What is done" at the end of this row.** SPX lists two options for each third Friday: the traditional monthly settling at the OPEN (stops trading the evening before) and the weekly SPXW settling at the CLOSE. Schwab returns both under one expiry key; the parser discarded the contract symbol and the uniqueness rule had no room for the difference, so one was dropped by `INSERT OR IGNORE` — exactly 160 rows per cycle, **2,181 identical warnings in `collector.log`**, for the whole life of the project. Both are now stored, tagged `settlement` = `AM`/`PM` (ADR-046). **What remains:** every read in `db.py` deliberately shows one contract per strike — the a.m. one where an a.m. one exists, otherwise the p.m. one — so nothing on screen changed, which means the shadowed third-Friday p.m. prices are now being recorded but cannot yet be seen. **Chandan's decision, 2026-08-19: each contract becomes its own entry in the expiry lists, with the p.m. one UNLABELLED (`21 Aug 2026`) and the a.m. one marked (`21 Aug 2026 (AM)`).** That way round because p.m. is the normal case — every other date in the list is p.m. already — so the label marks the exception rather than the rule. Consequence: an expiry stops being a date and becomes a date *plus* which contract, and that pair must travel everywhere the date travels — both dropdowns, the strike table, the charts, the scanner, saved positions and the journal, ~20 files. **Build the saved-position expiry sweep first**: it parses the label as a plain ISO date and is the one path that deletes things. It already refuses to delete a lock it cannot parse, so the failure mode is a stale entry rather than a lost one — but it is still the piece to check hardest. **Note the asymmetry:** p.m. history begins 2026-08-19 — for rows RECORDED before then the attribution rule in BUG-024 applies, and on a monthly they belong to the a.m. contract, so the p.m. series genuinely starts today. **What is done (2026-08-19):** `core/contract.py` holds the key and the legacy-attribution rule; `core/expiry.py` (the one path that deletes records) parses it; `db.get_option_chain` returns both contracts; `get_contract_iv_history`, `get_diagonal_history`, `get_transform_mark_history` and the entry-IV context all take a key; `dataaccess/queries.py` builds the key at the load boundary and keeps `expiry_date` beside it; `core/format.exp_label` marks the a.m. one; `core/pins.py` gained `expiry_dates` so a labelled lock still pins its date; `ui/controls.py` compares dates rather than keys. Verified read-only on the live 2.89 GB file: 21 dropdown entries, and at 7750 C the p.m. contract marks 9.05 against the a.m. 6.95. **What remains:** the journal's manual leg forms, which take a typed date and are not fed from the dropdowns, so a journalled a.m.-contract trade records nothing about which contract it was. Everything else is done: the daily ATM-IV summary gained its own `settlement` column on 2026-08-19 (ADR-047), and the four damaged summary snapshots were rebuilt from their intact `option_rows` the same day. Verified on the live 3.10 GB file: the a.m. contract reads 9.51% against the p.m. 10.11%, and their marks 5.75 against 15.10. |
| DEBT-039 | P3 | **Two scripts each carry their own copy of the "make stdout printable" shim, and the second one only exists because the first one's lesson was not carried across** | `check_db.py._force_utf8_stdout` was written for BUG-018 in July, for exactly the failure that killed the watchdog as BUG-029 on 2026-09-03: a redirected stdout on Windows is cp1252 and cannot encode the characters these reports are drawn with. **The fix existed in the repo for six weeks and the watchdog was written without it.** There are now two copies, and `watchdog._say` additionally guarantees that printing cannot stop an alert — which `check_db.py` does not need and should not grow. **Fix:** one shared helper (`core/` may not import `config`, so it belongs somewhere like `core/output.py`), used by both, with the alert-safety wrapper left in the watchdog. Small; the value is that the third script gets it for free. |
| DEBT-040 | P3 | **Two launch mechanisms for the collector, and only one of them is real** | Opened 2026-09-03 after **BUG-031 was raised in error and withdrawn the same day**. The collector *does* start with Windows — Chandan said so from direct observation, and he was right. It starts from a **Startup-folder shortcut**, `%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\SPX Diagonal Collector.lnk`, created 22 June (the day before collection began), enabled in Task Manager, targeting `.venv\Scripts\python.exe collector.py` — which is exactly the command line of the process found running today. **The mistake was looking in one place and concluding from its emptiness**, the same shape of error as an audit check that finds nothing and is read as proof. What is true is only this: **`scripts/register_collector_task.ps1` registers a Task Scheduler entry that does not exist and is not used**, so the repo documents a mechanism the machine does not run, and a reader comparing the two would reasonably reach the wrong conclusion — as happened. The script is not wrong, merely unused; the scheduled-task route also survives Smart App Control issues the shortcut historically hit (see the script's own header). **Fix:** decide which one is the real mechanism, say so in `README.md`/`STATUS.md`, and either delete the unused script or mark it clearly as an alternative. **No behaviour change and no urgency — startup works today.** |
| BUG-024 | P3 | Past monthly-expiry days have a seam in the record that nothing labels — **but the record CAN be read correctly; the earlier 'not back-fillable' finding was wrong** | Before the fix, on a monthly the stored row was the a.m. contract on ordinary days, but on the expiry day itself the a.m. option had already settled out of the broker's chain and the p.m. contract silently took the slot. Measured on the 17 July monthly: total call open interest ran 148,989 → 266,366 through the month and read **99,194** on expiry day — a different option, not a collapse in interest. **Correction, 2026-08-19:** the attribution is deterministic and needs no guessing. A weekly ever had only one contract (p.m.); on a monthly, a row recorded BEFORE expiry day is the a.m. contract and one recorded ON expiry day is the p.m. one. Proved by matching the unlabelled 21 Aug rows against both labelled contracts on open interest, which does not move intraday: **170 of 170 matched a.m., 0 matched p.m.** So the seam is labellable and the a.m. contract keeps its full history. Do this **at read time** — the stored NULL stays honestly "not recorded", and nothing becomes irreversible. Folded into the BUG-023 display work. |
| BUG-018 | P2 | **On expiry day the Entry tab tells you to set strikes that are already set** | Found 2026-07-30 during M2 step 2.4, by reading the values the before/after render comparison captured rather than only its verdict — the move itself changed nothing, and the two dumps are identical. `atm_straddle_price()` correctly returns `None` when `dte <= 0` (`iv_engine.py:487`), and the Front Expiry selectbox defaults to `index=0`, the **nearest** expiry. So on any day an expiry is listed for that same day — today, 2026-07-30, front expiry `2026-07-30`, 0 DTE — the default selection yields no straddle, and `normalized_debit()` returns `None` with it. The Normalized Debit tile then renders its fallback string, which is **`"— (set strikes)"`**, while the Diagonal Mark tile beside it shows a real 26.87 pts from those very strikes. The instruction is not just unhelpful, it is false, and it points at the wrong control: nothing about the strikes is wrong. Same family as BUG-010 — a value that could not be computed is displayed as though an input were missing. **Also affected:** the Research tab drops its amber "Current" diamond on those days, since that too is gated on `_norm_deb is not None`. **Fix (Chandan's call, it is a wording decision):** distinguish "no strikes chosen" from "front expiry is 0 DTE — no straddle to normalise against", the second being normal and temporary rather than something to act on. Cheap to do while `views/entry.py` is open. | M2.5 |
| BUG-013 | P2 | Dead duplicate of `total_fees()` body stranded inside `resolved_pl()` | Found while reading for the M1.3 tests, 2026-07-26. `pages/journal.py:290-298` is an orphaned copy of `total_fees()`'s docstring and body sitting **after** `return None` at the end of `resolved_pl()` — unreachable, and evidently a `def` line lost in an edit. Harmless at runtime but actively misleading: it reads as if `resolved_pl()` computes fees. Delete during M2.13. |

---

## 🔧 Technical Debt

### Must clear before major feature work

| ID | P | Item | Milestone |
|---|---|---|---|
| DEBT-029 | P1 | **The dashboard depends on two Streamlit APIs whose removal dates have already passed** | Found 2026-07-30 by running the app after the M2 step 2.1 extraction; unrelated to it. Streamlit 1.58 prints both on every startup: `use_container_width` (**33 uses** in `app.py` + `pages/journal.py`) was to be removed after **2025-12-31**, and `st.components.v1.html` (**1 use**) after **2026-06-01**. Both dates are in the past — the dashboard runs only because the compatibility shims have outlived their notice, and any Streamlit upgrade may remove them without further warning. Not urgent while the version is pinned, but it means **the version is now effectively pinned whether or not that was decided**. **Fix:** `use_container_width=True` → `width='stretch'`, `=False` → `width='content'`; `st.components.v1.html` → `st.iframe`. Mechanical, but touches 34 call sites, so it wants doing in one pass — natural fit with `views/` (2.4), where most of the call sites will be moving anyway. **UPDATE 2026-07-30: 2.4 is done and the call sites have landed** — they are now spread across the six `views/*.py` modules and `pages/journal.py`, not one file, which makes this slightly more tedious and no harder. **UPDATE 2026-07-31: 2.5 added `ui/` and `services/`** — `use_container_width` now also appears in `ui/sidebar.py` and `ui/locks.py`, and the single `st.components.v1.html` call is `ui/header.py`. Both deprecations still print on every startup. Nothing blocks it. Revisit at M5.0 if the migration-away-from-Streamlit question reopens. | M2.5 |
| DEBT-035 | P3 | **The card sparkline is still computed but no longer shown** | Opened 2026-07-30 as a second DEBT-031 — two unrelated items were given that number the same day. **Renumbered to DEBT-035 on 2026-07-31**; the threshold item keeps DEBT-031 because an ADR already cites it. The 10-glyph gap sparkline was removed from the Transform Opportunities cards at Chandan's request — the same information is what "View Chart" opens, properly drawn, so a squashed copy on every card was clutter. Only the *display* went. `_candidate_signals` still computes `spark` via `core.format._sparkline`, `_compute_mc_core` still carries it on every card, and both are pinned by tests in `test_mission_control_golden.py` and `test_mc_pipeline_golden.py`. Left deliberately: deleting the field means editing those existing tests, which is a different kind of change from removing a `<div>`, and the cost of computing it is one pass over 12 numbers per card. **Fix:** drop `spark` from the pipeline and retire its tests together, in one deliberate commit — or keep it, if the sparkline might return elsewhere. Ask before doing either. | — |
| DEBT-037 | P3 | **~50 lines of KPI-card CSS now style nothing** | Orphaned 2026-08-01 by ADR-043, which deleted the Scanner's KPI card row at Chandan's request. `assets/theme.css` still carries `.kpi-grid`, `.kpi-card`, `.kpi-card::after`, `.kpi-card:hover`, `.kpi-card.kpi-hl`, `.kpi-icon`, `.kpi-v` (+ `.c-blue`/`.c-amber`/`.c-green` variants), `.kpi-l` and `.kpi-sub` at roughly lines 496–546. **`grep -rn "kpi-" --include=*.py` now returns nothing** — no Python builds those class names any more. **Left deliberately, and not just from caution:** it is the cheapest possible restore path for ADR-043's decision, which is the one thing that decision is short of. Dead CSS costs bytes on one page load and nothing else. **Fix:** delete the block — but only once Chandan is sure he does not want the cards back, since deleting it turns a paste-one-block restore into a rewrite of the styling too. Ask first. | — |
| DEBT-038 | P3 | **Closed-item IDs are being reused, which breaks the documented way to recover them** | Found 2026-08-01 while fixing BUG-019. ADR-017 says closing an item means DELETING its row, and that `git log -S "BUG-011" -- docs/backlog.md` is how the text is recovered later. That only works while an ID is unique. **It is not: `BUG-019` has been used twice** — once for the health check counting "snapshots today" in UTC (closed 2026-07-28 by ADR-027) and again for the Scanner's missing KPI cards (opened 2026-07-30, closed 2026-08-01 by ADR-043). A `git log -S "BUG-019"` now returns two unrelated bugs interleaved, and nothing in the output says they are different items. **This is the second occurrence of the pattern:** DEBT-031 was issued twice on 2026-07-30 and one was renumbered to DEBT-035. That one was caught the same day; this one ran for three days. **Fix:** record the highest ID ever issued per prefix somewhere a human will see before opening a row — a one-line "next IDs" note at the top of this file is probably enough. The alternative, never reusing by checking `git log` first, is the rule that has now failed twice. | — |
| DEBT-036 | P3 | **`pinned_pairs.json` in the project root is a dead file** | Found 2026-08-01 while fixing BUG-022, and named here only because the *filename* is now actively misleading: BUG-022 introduced real strike/expiry pinning, which reads `entry_locks.json` and has nothing to do with this file. This one is an orphan from the v2 dashboard (`0c2e398`, "pair scanner, pinned pairs"), where it held a watchlist of expiry pairs. **No Python reads it** — `grep -rn "pinned_pairs" --include=*.py` returns nothing — and its contents are two expiries from July 2026, long past. The 2026-07-25 audit already listed it as an orphaned feature (§2.4, and the `.gitignore` corruption entry). **Fix:** delete the file. Not done during BUG-022 because deleting files needs Chandan's word, and the file is 82 bytes of nothing — there is no cost to it sitting there except the name collision, which this row now covers. | — |
| DEBT-031 | P2 | **The 5-point transform threshold is written out four times, and two of the copies disagree** | Found 2026-07-30 while reading the Entry tab for M2 step 2.4; not caused by it, and deliberately NOT fixed there, since a move that changes a threshold is no longer a move. `core/scanner.py` defines `_TSCAN_THRESHOLD = 5.0` and the Scanner, the eligibility registry and the crossing log all use it. The Entry Analysis tab uses none of them: it hardcodes `_ic_signal > 5` **twice** for the green "✓ Transformation favorable" tick, and `_THRESHOLD = 5.0` with `_diff >= _THRESHOLD` for the progress bar beneath it. **Two consequences, one already live.** *Today:* at a difference of exactly 5.00 the two panels in the same tab contradict each other — the progress bar reads "threshold reached" while the tick above it stays absent, because one test is `>=` and the other is `>`. The Scanner agrees with the bar, so the tick is the odd one out. *The day the threshold is tuned:* changing `_TSCAN_THRESHOLD` moves the Scanner and the registry and leaves the Entry tab flagging the old number, so a card the Scanner lists as live reads as unfavorable on the tab you open to check it — with nothing anywhere reporting a disagreement. **Fix:** import `_TSCAN_THRESHOLD` in `views/entry.py` and settle `>` vs `>=` deliberately (`>=` matches everything else). Pin the boundary at exactly 5.00 first — that is the case that is wrong now. **The tab is now `views/entry.py`, and importing the constant there costs one line.** | M2.5 |
| DEBT-033 | P3 | **The extracted `services/` names still carry app.py's leading underscores** | Opened 2026-07-31 (ADR-041). `services/loaders.py` and `services/sidecars.py` export `_load_atm_hist`, `_load_entry_locks`, `_save_chart_colors` and eleven more — private-by-convention names imported across module boundaries by `app.py` and `ui/locks.py`. They kept their names ON PURPOSE so each move could be proved character-identical to what it replaced, which is the same order DEBT-028 used for `core/`: move first, rename in a separate commit whose diff shows nothing but the rename. **Note the trap that makes this worth doing deliberately:** `tests/app_loader.py` finds these by name, so a rename must update `_PIPELINE_FUNCS` in the same change or 41 golden tests fail with a misleading "no source defines" message. `exp_label` was already done this way during 2.5 as the pattern. | — |
| DEBT-034 | P3 | **`spx_intraday` is loaded and timezone-converted for a column nothing reads** | Found 2026-07-31 while extracting the prelude for step 2.5; not caused by it, and deliberately NOT fixed there — a move that deletes code is no longer a move (the DEBT-032 lesson from 2.4, where half the "dead" code turned out to be BUG-019). `app.py` calls `_load_spx_intraday(...)`, then builds `spx_intraday["ts_et"]` through a full `to_datetime` + `tz_convert` + `tz_localize` pass. **`ts_et` is never read anywhere.** The frame's only surviving use is `underlying_price.iloc[0]` as the daily-change fallback when there is no prior close — one scalar. Almost certainly left behind when an intraday SPX chart was removed. **Cost:** one query and one datetime conversion over a session's snapshots on every rerun, for a single number. **Fix:** either drop the `ts_et` block and keep the read, or replace the whole thing with a scalar query for the session's first `underlying_price`. Check git history for the removed chart first — BUG-019 is the standing reminder that "dead" and "dropped" look identical. | — |
| DEBT-001 | **P0** mostly cleared | ~~No automated tests anywhere (9,628 lines)~~ — **450 tests** as of 2026-07-26, run on every commit, and the codebase is linted. `iv_engine`, `db.py`, `schwab_client.py` and `config.py` at **100% statement coverage**; `collector.py` covered except `main()`'s body; the journal P&L maths, a scanner golden net and token expiry all covered. **80% of non-UI code overall.** Every module mutation-verified rather than assumed. **Still bare: `app.py` and `pages/journal.py` beyond the extracted pure functions** — that is M2 work, and the reason ADR-015 still defers the formatter. | M1 |
| DEBT-003 | **P0** | Unbounded DB growth (~82 MB/trading day, ~20 GB/yr) | M3 |
| DEBT-004 | **P0** | `$5.00` transform threshold duplicated ×4 as literals (`app.py:1575, 3103, 3686, 3704`); not in `config.py` | M2.2 |
| DEBT-005 | **P0** | `DOCUMENTATION.md` drifted from code — see DEBT-010 | M2.14 |
| DEBT-006 | **P0** | `±5` wing offset hardcoded in `app.py:2559` **and** inside SQL in `db.py:815-900` | M2.2 |

### Clear during refactor

| ID | P | Item | Milestone |
|---|---|---|---|
| DEBT-007 | P1 | Silent exception swallowing in P&L display paths | M2.13 |

> **DEBT-007 detail (measured 2026-07-26, first real ruff run).** The estimate is now exact: **4 bare `except:`** (`pages/journal.py` 1298, 1507, 1546, and one more) and **~30 blind `except Exception:`** across `journal.py` (18), `app.py` (3), `collector.py` (3) and `db.py` (2). These swallow every error silently, so a failed P&L calculation renders as a blank cell indistinguishable from a real value — the same confusion as BUG-010. `ruff check --select BLE001,E722` reproduces the list with line numbers. Note `collector.py`'s and `db.py`'s are deliberate and now carry a comment saying so; `journal.py`'s are not.

| DEBT-008 | P1 (was P0) — **half fixed** | ~~The caller is told discarded rows were stored~~ — fixed 2026-07-26 (M1.5): `insert_option_rows()` now returns `cursor.rowcount` and logs a WARNING naming the shortfall. **Still open:** `OR IGNORE` continues to discard constraint-violating rows rather than raising, and the collector still records `strikes_fetched = len(option_rows)` (the offered count) rather than the stored one. Per-constraint behaviour is the M3.6 decision (ADR-022 step 2). | M3.6 |

> **DEBT-008 detail (raised to P0, 2026-07-26, M1.5 tests).** The severity in ADR-004 was understated. `OR IGNORE` is not scoped to uniqueness — SQLite applies it to *every* constraint on the statement, so a `CHECK` or `NOT NULL` violation skips the row rather than raising. `insert_option_rows()` compounds this by returning `len(rows)`, computed before the statement runs. **Combined failure mode:** if Schwab ever changed its `right` convention from `'C'` to `'CALL'`, every option row would be silently discarded, `insert_option_rows()` would return 3,096, and `collector.log` would record a full healthy cycle — indefinitely, until someone noticed the charts had stopped moving. Nothing in the system would say otherwise, and the missing data is unrecoverable (the broker will not sell you last Tuesday's prices). **Minimum fix, cheap and independent of the M3.6 refactor:** compare `cursor.rowcount` against `len(rows)` and log any shortfall as a warning. Pinned by `test_insert_option_rows_silently_discards_a_row_failing_the_check` and `test_insert_option_rows_keeps_the_good_rows_when_one_is_bad`.
| DEBT-009 | P1 | 757-line CSS embedded in `app.py`, targeting Streamlit-internal DOM classes | M2.7 |
| DEBT-010 | P1 | Doc drift: `transform_credit()`/`calendar_edge()` documented as core but never called; Entry Locks undocumented; doc stops at v4.1; §14.8 wrongly claims `chart_colors.json` is gitignored; Pinned Pairs/Pair Scanner documented but removed in v3.3 | M2.14 |
| DEBT-011 | ~~P1~~ **P3, reduced** | Sidecar JSON (`eligible_history.json` ~700 KB, `entry_locks.json`, `chart_colors.json`). **Three of five parts fixed 2026-07-30 in M2 step 2.3 (ADR-035)** — the three that could lose data. ~~paths are relative~~ → `config.STATE_DIR`, absolute, anchored to the project root exactly as `DB_PATH` always was. ~~no atomic write~~ → written to a temp file and renamed; the registry is rewritten in full ~126 times a trading day. ~~no validation~~ → an unreadable or non-object file is quarantined as `<name>.corrupt-<timestamp>` instead of being read as `{}` and then overwritten, which was **data loss on a delay**. **STILL OPEN: (a) no formal schema** — the check is "is it an object", not "does each entry have the right fields"; **(b) no scheduled backup** — belongs with M3's backup work, and note all four sidecars are gitignored so there is no copy in version control. | M3 |
| | | *Added 2026-07-29 (ADR-031): `_ELIGIBLE_HISTORY_PATH = Path("eligible_history.json")` resolves against the **working directory**, not the project. Launch the dashboard from anywhere but the repo root and the registry silently starts empty — no error, just an eligibility history that has forgotten everything. Same pattern for `entry_locks.json` and `chart_colors.json`. Make them module-relative, as `collector.py`'s log path already is (M0.12). This also means a test that forgets to redirect the path overwrites the live 599 KB file, which is why `load_pipeline()` takes it as an argument.* | |
| DEBT-012 | P1 | Duplicated helpers: `_safe_float` ×2, mark-fallback `(bid+ask)/2` ×**5**, expected-move calc ×2 | M2 |
| DEBT-013 | P1 | No logging in `app.py`/`journal.py` — loggers created, never called | M0.12 |
| DEBT-014 | P1 | ~6 dangling citations to the deleted June audit in `DOCUMENTATION.md` §3.1/§9.4/changelog and `iv_engine.py` lines 81-84, 110-111, 360-361 (ADR-011). Recovery SHA `6329fa28` | M2.14 |
| DEBT-015 | P2 | Dead code: ~13 functions + `demo_data.py` + `DEMO_MODE` + 3 legacy DB tables | M0.11 |
| DEBT-016 | P2 | Redundant indexes: `idx_option_rows_contract` (218 MB), `idx_option_rows_snapshot_id` (100 MB) | M0.10 |
| DEBT-017 | P2 | Code-generator artifact string at `db.py:1039-1045`; mid-file `import json as _json` | M0.11 |
| DEBT-018 | P2 | `_run_mission_control` ~170 ln; Calendar Edge tab body ~750 ln | M2.10 |

### Opportunistic

| ID | P | Item |
|---|---|---|
| DEBT-025 | P2 | **85 ruff findings remain after the first lint pass** | Config was written in M0 but ruff was never installed, so nothing had ever been linted. Installed 2026-07-26; noise families silenced, 55 mechanical items auto-fixed, **85 judgement calls left**: 18 PLR0917 (too many positional args — pervasive in `db.py` readers), 12 PLC0415 (imports not at top), 12 E741 (variables named `l`, in `journal.py`'s leg parsing), 8 SIM118, 6 SIM105, 4 E722 + 4 B905 + 3 PLW0603, 2 F841. None is a defect; the `except` ones belong to DEBT-007. Mostly lands naturally during the M2 decomposition. **Do not wire ruff into the pre-commit hook until this is at zero** — a gate that fails on every commit teaches everyone to bypass it. |
| DEBT-019 | P2 | `README.md` fully obsolete (describes an MVP scaffold, references nonexistent `.env.example` flow, Demo Mode) |
| DEBT-020 | P2 | `start_collector.bat` hardcodes absolute paths; venv lives *outside* the project at `C:\Users\chand\Python\.venv` and is shared |
| DEBT-021 | P2 | `collector.log` unrotated (486 KB and growing) |
| DEBT-022 | P3 | No `LICENSE`, no `CONTRIBUTING.md` |
| DEBT-023 | P3 | No `__version__` / VERSION file; "v4.2" exists only in a commit message |
| DEBT-024 | P3 | Deleted `trade_forensics_2026_06_23.ipynb` (1,397 ln) no longer discoverable; in history only |

---

## ⚙️ Operations

| ID | P | Item |
|---|---|---|
| ~~OPS-001~~ | ~~P0~~ | ~~`register_collector_task.ps1` was never registered — collection depends on manual start.~~ **CLOSED 2026-07-26 — the audit finding was wrong.** Auto-start has worked since 2026-06-22 via a **Startup folder shortcut** (`shell:startup` → `.venv\Scripts\python.exe collector.py`, working dir = project root). Verified: shortcut intact, all paths resolve, collector running continuously since the 7/16 logon with full 126-snapshot sessions daily. `DEV_JOURNAL.md` 2026-06-22 documents why Task Scheduler was rejected — the `.bat` was blocked by Windows Smart App Control and the PowerShell script failed without admin rights (reproduced 2026-07-26: `Register-ScheduledTask` and `schtasks /sc onlogon` both return Access Denied, because ONLOGON triggers require elevation). The Startup shortcut is the correct solution for this machine; do not replace it. |
| OPS-001b | P1 | **No crash recovery for the collector.** The Startup shortcut starts it at logon but nothing restarts it if the process dies mid-session — the capability Task Scheduler's `RestartCount` would have provided. This is the *real* residual gap behind the original OPS-001 concern, and it is much smaller: a crash costs part of one session, not every session. Pairs with OPS-003. |
| OPS-002 | **P0** | Backups now exist (M0.1) but are **manual**. Automate + rotate. |
| OPS-003 | P1 | No collector liveness alerting — if it dies mid-session, only a staleness dot changes |
| OPS-004 | P1 | Schwab token re-auth is a manual weekly copy-paste chore; needs a documented runbook |
| OPS-005 | P1 | `collection_gaps` is populated but never surfaced anywhere in the UI. **Unblocked 2026-07-26** — BUG-005 is fixed and the historical rows reclassified (ADR-024), so the table is now trustworthy enough to render: 28 real faults rather than 47 false alarms. See OPS-008 before building the view. |
| OPS-008 | P2 | **Decide what to do with the ~22 session-change artefact rows in `collection_gaps`** | They record non-events: an ordinary 5-minute MIDDAY interval judged against the 60s CLOSE threshold at the 15:30 session change (ADR-024). The collector no longer creates them. The existing rows were deliberately left alone by `migrations/reclassify_collection_gaps.py` — deleting rows from an audit log is a judgement call, not a migration's business. Options: delete them, or add a distinct `reason` such as `CADENCE_ARTEFACT`. Wants deciding before OPS-005 renders the table. |
| OPS-007 | P2 | No dependency vulnerability scanning (`pip-audit` now in dev deps, not yet wired) |
| DEBT-031 | P2 | **Mission Control's panel cannot be served, and the reason is structural.** `services/mission_control.py` computes the cards, sparklines, "likely next" list and gap durations, and every path through it imports streamlit — it reads `st.session_state` and its two phases are `@st.cache_data`. `api/` is forbidden from importing `services/` (enforced), so M4.3 served the scanner sweep and the "New" flag, which come from `core/` and are the inputs the panel DISPLAYS, and left the panel itself alone. Copying 500 lines into a second home that would immediately drift was the alternative and is worse. **What it needs:** the same extraction M2 did — the panel's logic into a layer both the page and the API can call, with the streamlit-bound memo and session state left behind in `services/`. Sized like an M2 step, not a bug fix. | M5 |
| DATA-001 | P2 | **Strike coverage is narrower than the config implies, and the collector's own warning misdirects the fix.** Measured 2026-09-05 against 2026-09-04 data. Every expiry returns exactly 80 distinct strikes — `STRIKE_COUNT = 80` is 80 *total*, not 80 per side — so at spot 7718 with 5-point spacing the near-dated expiries reach only **±198 points** and `STRIKE_FETCH_WIDTH_POINTS = 300` never fires on them. At 17–28 DTE the wing spacing widens to 25 points, the broker does supply past ±300, and the width filter clips it at exactly ±300 both sides: **wing data we already paid for in the same API call is discarded**. Coverage on the 28 DTE long leg is therefore ~1.75 SD, against ~2.3 SD on the near legs. The `WARNING 2 SD expected move ... Consider widening STRIKE_FETCH_WIDTH_POINTS` line (`schwab_client.py`) is the symptom, but its advice alone would change nothing for near-dated expiries. **Not a live risk to open trades** — locked legs are already exempt from the window (BUG-022). **Chandan's decision 2026-09-05: leave it alone for now**; the cost of widening (~82 MB/day → ~120 MB/day) runs against what M3 was for. Revisit if analysis or backtesting needs the wings. |
| OPS-009 | P3 | **The collector's INFO logging never reaches disk.** `filter_chain_by_strike_window` logs `"strike window: broker supplied -X/+Y pts around spot ... (headroom ...)"` at INFO, written specifically because the filter destroys the evidence of how wide the broker actually went. It appears **zero times** in `collector.log`. Found 2026-09-05 while investigating DATA-001, which had to be reconstructed from the database instead. Worth knowing what else is being logged into a void before relying on a log line as evidence. |

---

## ✨ Enhancements

| ID | P | Item | Milestone |
|---|---|---|---|
| ENH-011 | ~~**P1**~~ **DONE 2026-09-05 (M5.0)** | ~~**Tab clicks take noticeably long — every click re-runs the whole page from the top** | Requested by Chandan 2026-07-30, immediately after BUG-020 was fixed: *"Your fix made the dashboard navigatable again... I'd like to speed up the navigational ability when I click anywhere on the dashboard."* Distinct from BUG-020 — that was an infinite loop that never finished; this is a page that finishes, just slowly. **Cause not yet established.** The known shape of the problem: the nav bar is a session_state button row that calls `st.rerun()`, so a tab click re-executes `app.py` end to end, including the chain load and the Mission Control computation, and only then draws a different tab body. Memoisation exists (`@st.cache_data`, 15 wrappers) but the TTLs are short — 55s on the per-snapshot reads, 120s on the scanner sweep — so a click landing after a TTL lapse redoes real work even though the snapshot has not changed. **Do not start by tuning TTLs.** Measure first: time a click end to end, then attribute it (chain load / MC compute / chart serialisation / browser render). The 1,208-row "approaching" set and six Plotly charts are both plausible and neither is confirmed. Note that TTL-based invalidation is arguably the wrong key here anyway — the data changes on a new *snapshot*, not on a clock. **MEASURED 2026-09-05 (M5.0), see docs/m5_measurement.md.** Streamlit's own overhead is ~0.04s per rerun — the framework is not the cause. The costs are ours: a fixed ~0.15s prelude on every click (Mission Control ~0.09s of it), and per-tab query time of 0.8–2.7s on first visit. The TTL suspicion above is **confirmed with a number**: Gamma Exposure cost 1.09s, 0.245s on immediate return, and 0.881s again after 65s idle with the snapshot unchanged — 0.65s spent recomputing a byte-identical answer because the clock moved. The fix is to port `api/cache.py`'s snapshot-keying into `services/loaders.py`; no migration off Streamlit is implied by the measured evidence. **Not measured:** browser paint and websocket delta — `AppTest` sees server-side script time only, and playwright/selenium are not installed.~~ **FIXED the same day:** all TTLs removed; `invalidate_on_new_snapshot()` drops the memo when the collector writes a new snapshot and at no other time. Prior close and prior-session OI are deliberately excluded (immutable, already keyed by session date). Re-measured: Strike 2.66s→0.30s, Research 1.59s→0.21s, Edge 0.85s→0.25s, and the 65s-idle penalty on Gamma Exposure 0.881s→0.133s. Covered by tests/test_loaders_invalidation.py; both guards proved by breaking them. | M2.5 |
| ENH-001 | P1 | ~~FastAPI layer over `core/` — needed for mobile access (ADR-012)~~ **DONE 2026-09-05 (M4.1–4.5).** Reads, scanner, gamma, push and a shared token; `api/` is guarded against importing streamlit. **Not served: the Mission Control panel** — that logic is 528 streamlit-bound lines in `services/`, and extracting it is its own task (see DEBT-031). | M4 |
| ENH-002 | P1 | ~~WebSocket push on new snapshot, replacing polling~~ **DONE 2026-09-05 (M4.4).** `/ws/snapshot`. Honestly still polling, but ONE poller in the server rather than one per client, which is the change asked for. | M4.4 |
| ENH-003 | P2 | Threshold calibration from real fills ($5.00 → ~$6.50–7.00); needs ≥10 transformations | M6.1 |
| ENH-004 | P2 | Complete Regime Analysis validation; needs ~20+ trades (**currently 6**) | M6.2 |
| ENH-005 | P2 | Net Theta Advantage / time-to-viability from per-leg Greeks — the honest replacement for the removed Theta ETA | M6.4 |
| ENH-006 | P2 | Payoff diagrams: diagonal (BS pre-expiry) + IC (intrinsic at expiry) | M6.5 |
| ENH-007 | P3 | Backtest engine: replay stored IV history against entry rules | M6.3 |
| ENH-008 | P3 | In-place chart updates preserving zoom/pan across new data | M5.3 |
| ENH-009 | P3 | Remove ▲/▼ carets from the Diagonal/Transform chart (low information once data is clean) | M5 |
| ENH-012 | ~~P2~~ **DONE 2026-09-05 (M5.0)** | ~~**Mission Control is computed on every tab, including tabs that never show it** — ~0.09s of the fixed prelude tax measured in M5.0. It runs unconditionally because the header's Attention Strip needs it. Making it conditional is not simply an `if`: the strip is on every tab. The cheaper shape is to memoise it on the snapshot alongside ENH-011 rather than to skip it. Raised from evidence, 2026-09-05.~~ **FIXED the same day.** The real cost was larger than first measured: `_build_non_atm_panel` walked all 1,286 registry entries on every click to show 20 — 0.46s, not 0.09s (the registry grew between the two measurements, and the cost scales with it). Now memoised on (snapshot, lookback) via `_build_non_atm_panel_cached`. Remaining, and not urgent: registry size still scales the once-per-snapshot compute. | M5 |
| ENH-010 | P3 | Full Journal integration for entry locks (`journal_trade_id` scaffold already present) | M6 |

---

## 💡 Future Ideas (unscheduled, not committed)

- Data-quality flagging: IV outliers, stale quotes, missing legs
- Narrower "hot" table (`snapshot_id, expiry, strike, right, mark, iv`) for chart queries
- Mobile-specific layout once M5 lands (ADR-012 makes this likely to matter)
- Desktop notifications when a transform threshold is crossed
- Schwab Streamer (websocket) API instead of polling — only if polling ever feels slow
- VIX term structure as an additional regime dimension

---

<!-- No "Recently Completed" section here (ADR-017): progress_log.md, plan.md and git already record done work. -->

